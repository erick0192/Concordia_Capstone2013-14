<UserControl x:Class="RoverOperator.Content.MotorStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" FontSize="14">
    <Grid>
        <StackPanel>        
            <StackPanel Orientation="Horizontal">
                <Image Source="../Assets/icons/mechanicalwheel.png" Width="64" Height="64" HorizontalAlignment="Left"/>        
                <StackPanel Margin="10,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Name="Current" Text="{Binding StringFormat='Current: {0}', Path=Motor.Current, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Name="Temperature" Text="{Binding StringFormat='Temperature: {0}', Path=Motor.Temperature, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
