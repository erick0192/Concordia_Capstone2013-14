<UserControl x:Class="RoverOperator.Content.IMUView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300" FontSize="12">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Path=IMUSensor.Yaw, StringFormat='Yaw: {0}'}" Margin="0,0,10,0" />
            <TextBlock Text="{Binding Path=IMUSensor.Pitch, StringFormat='Pitch: {0}'}" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Path=IMUSensor.Roll, StringFormat='Roll: {0}'}" Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</UserControl>
