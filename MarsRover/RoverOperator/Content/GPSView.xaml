<UserControl x:Class="RoverOperator.Content.GPSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300" FontSize="12">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Path=Coordinates.X, StringFormat='X: {0:0.000000}'}" Margin="0,0,10,0" />
            <TextBlock Text="{Binding Path=Coordinates.Y, StringFormat='Y: {0:0.000000}'}" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Path=Coordinates.Z, StringFormat='Z: {0:0.000000}'}" Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</UserControl>
