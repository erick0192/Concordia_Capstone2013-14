<UserControl x:Class="MarsRoverClient.Content.BatteryStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Margin="10,0,0,0">
    <StackPanel>            
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Path=Battery.ChargePercentage, StringFormat='Charge: ({0}%)'}" />
            <ProgressBar Minimum="0" Maximum="1" Value="{Binding Path=Battery.ChargeRatio, Mode=OneWay}" Height="16" IsIndeterminate="False" Margin="10,0,0,0" Width="50" HorizontalAlignment="Left" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Path=Battery.Temperature, StringFormat='Temperature: {0} C'}" />            
        </StackPanel>
    </StackPanel>
</UserControl>
